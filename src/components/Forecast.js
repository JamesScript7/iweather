import React, { Component } from 'react';
import { loadWeather } from '../actions';
import { Card } from 'react-materialize';

// Styles
import './Forecast.css'

class Forecast extends React {
  constructor(props) {
    super(props);
  }
  render() {
    return (

    );
  }
}

export default Forecast;
